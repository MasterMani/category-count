import request from "request"
import request from r"cheerio");

class fetchHtmlPage {
  
  constructor(url){
    this.urlList = urlList
  }

  const fetchingHtml= function(this.url) {
    new Promise()

  }

}